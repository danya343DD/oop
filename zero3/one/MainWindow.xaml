<Window x:Class="FileReaderProgress.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Чтение файла с ProgressBar" Height="300" Width="500"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Кнопка выбора файла -->
        <Button x:Name="BrowseButton" Grid.Row="0" 
                Content="Выбрать файл" Width="120" Height="30"
                Click="BrowseButton_Click" Margin="0,0,0,10"/>

        <!-- Информация о файле -->
        <TextBlock Grid.Row="1" Margin="0,0,0,10">
            <Run Text="Файл: "/>
            <Run x:Name="FileNameText" Text="не выбран" 
                 FontWeight="Bold"/>
        </TextBlock>

        <!-- ProgressBar -->
        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <ProgressBar x:Name="ReadProgressBar" 
                         Minimum="0" Maximum="100" Height="25"
                         Grid.Column="0"/>

            <TextBlock x:Name="PercentText" Text="0%" 
                       Grid.Column="1" Margin="10,0,0,0"
                       VerticalAlignment="Center" FontWeight="Bold"/>
        </Grid>

        <!-- Статистика -->
        <GroupBox Grid.Row="3" Header="Статистика" Margin="0,0,0,10">
            <StackPanel Margin="5">
                <TextBlock>
                    <Run Text="Всего символов в файле: "/>
                    <Run x:Name="TotalCharsText" FontWeight="Bold"/>
                </TextBlock>
                <TextBlock Margin="0,5,0,0">
                    <Run Text="Прочитано символов: "/>
                    <Run x:Name="ReadCharsText" FontWeight="Bold"/>
                </TextBlock>
                <TextBlock Margin="0,5,0,0">
                    <Run Text="Всего строк: "/>
                    <Run x:Name="TotalLinesText" FontWeight="Bold"/>
                </TextBlock>
                <TextBlock Margin="0,5,0,0">
                    <Run Text="Прочитано строк: "/>
                    <Run x:Name="ReadLinesText" FontWeight="Bold"/>
                </TextBlock>
            </StackPanel>
        </GroupBox>

        <!-- Кнопка запуска чтения -->
        <Button x:Name="ReadButton" Grid.Row="4" 
                Content="Начать чтение" Width="120" Height="30"
                Click="ReadButton_Click" IsEnabled="False"/>
    </Grid>
</Window>